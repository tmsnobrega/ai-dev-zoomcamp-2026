[project]
name = "household-chores"
version = "0.1.0"
description = "A small Django application for managing shared household chores."
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<6",
]
